import { Environment } from 'src/app/shared/interfaces/environment';

export const environment: Environment = {
  development: false,
  api: 'https://secureraz.herokuapp.com/',
  cookieDomain: '',
};
